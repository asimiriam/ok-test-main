
import {
    setNextStep,
    setPassword,
    setResultStep,
} from './nextStepSlice';
 
import {
    setLang
} from './langSlice';

import rootSlice from './rootSlice';

 export {
    setNextStep,
    setPassword,
    setResultStep,
    setLang,
}; 

export default rootSlice;
